import { styled } from '@material-ui/styles';

const StyledHelpText = styled('p')({
  '&.help-text': {
    color: '#8898aa',
    paddingLeft: 3,
    fontSize: '0.9375rem',
  },
});

export default StyledHelpText;
