import styled from 'styled-components';

const StyledForm = styled.form`
  margin-top: 20px;

  .fields {
    display: grid;
    grid-gap: 10px;
  }

  .btn {
    margin-top: 10px;
  }
`;

export default StyledForm;
