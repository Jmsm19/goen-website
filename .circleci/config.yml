version: 2.1

orbs:
  # react: 'thefrontside/react@dev:alpha'
  heroku: circleci/heroku@0.0.6

executors:
  default:
    description: |
      Node 10 runtime environment
    docker:
    - image: circleci/node:10

jobs:
  install_back:
    executor: default
    steps:
      - attach_workspace:
          at: ~/
      - checkout
      - restore_cache:
          key: v1-node-modules-{{ checksum "yarn.lock" }}
      - run: yarn install
      - persist_to_workspace:
          paths:
            - project
          root: ~/
      - save_cache:
          key: v1-node-modules-{{ checksum "yarn.lock" }}
          paths:
          - ~/project/backend/node_modules
  install_front:
    executor: default
    steps:
      - attach_workspace:
          at: ~/frontend
      - restore_cache:
          key: v1-node-modules-{{ checksum "yarn.lock" }}
      - run: yarn install
      - persist_to_workspace:
          paths:
            - project
          root: ~/
      - save_cache:
          key: v1-node-modules-{{ checksum "yarn.lock" }}
          paths:
          - ~/project/frontend/node_modules
  lint:
    executor: default
    steps:
      - attach_workspace:
          at: ~/
      - run: yarn setup
      - run: mkdir -p reports/eslint
      - run: yarn front eslint ./src/**/*.{ts,tsx} --format junit --output-file ../reports/eslint/eslint.xml
      - store_test_results:
          path: reports/eslint
      - store_artifacts:
          path: reports/eslint
  deploy:
    executor: heroku/default
    steps:
      - checkout
      - heroku/install
      - heroku/deploy-via-git:
          only-branch: master

workflows:
  push:
    jobs:
      - install_back
      - install_front:
          requires:
            - install_back
      - lint:
          requires:
            - install_front
      - deploy:
          requires:
            - lint